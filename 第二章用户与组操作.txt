第二章用户与组操作
●1、用户列表文件：/etc/passwd
●2、用户组列表文件：/etc/group
●一、用户操作             加       -l      显示帮助信息 (有哪些指令，并显示指令含义)
●添加用户(新用户ID在1000以上,1000以下留给系统)
●useradd   用户名               添加用户
●useradd   -d    /usr/samhome    -m  用户名    -d  指定用户登录的起始目录（登录以后所在的位置） -m登录时进入这个目录     
●useradd    -s   /bin/sh  -g   主分组名 -G   adm,root    用户名    -s  指定用户的登录shell   如果是 /bin/sh 的话是普通用户，可以登录   如果是/sbin/nologin 不允许登录可以如下使用，-g指定主分组，如果这个组不存在，需要先创建组。-G 指定用户所属的附加群组
●useradd 用户名 -s /sbin/nologin -M         -M不要自动建立用户的登入目录
●**useradd -r  用户名  **建立系统账号
●修改密码
●passwd [选项]  用户名（需要root账号操作）   如果没有用户名是修改当前登录的账号密码
●  -d           删除密码，仅有系统管理者才能使用   
●  -l          锁住账号   不允许登录
● -u              解开已上锁的账号
●修改用户账号
●usermod [选项 ] 用户名
●-d            修改新主目录
●-g             强制使用新主组
●-G             新附加组
●-l               新的登录名     usermod  -l   新的用户名    旧的用户名
●-L              锁定用户
●-p              修改密码        usermod   -p   新密码    用户名
●-U              解锁
●删除用户
●userdel     [选项]         用户名
●userdel   用户名         删除用户（只在passwd列表中删除）
●userdel   -r    用户名    删除用户所有信息（包括主目录和邮件）
●切换用户
●su     用户名
●二、用户组操作
●查看用户所在组及附加组      id   用户名
●添加用户组
●groupadd     [选项]      组名
●-g              指定组ID
●-r              创建系统工作组（系统工作组ID小于500）
●修改用户组
●groupmod     [选项]     组名
●-g            修改组ID
●-n            修改组名   groupmod   -n    新名   旧名
●用户在用户组之间切换
●newgrp    组名      使用 newgrp root命令将当前用户切换到 root用户组，前提条件是 root用户组确实是该用户的主组或附加组。
●删除用户组
●groupdel     组名
●三、文件与目录操作（权限操作）
●使用  ls   -lh

●文件属性关系

●属性中的第一个字母（权限）
●当为d时，则是目录。
●当为- 时，则是文件。
●若是l 时，则表示为链接文档（link file）。
●若是b 时，则表示为装置文件里面的可供储存的接口设备（可随机存取装置）。
●若是 c时，则表示为装置文件里面的串行端口设备，如键盘和鼠标（一次性读取装置）
●接下来的字符中，以 3个为一组，且均为 [ rwx ]的 3个参数的组合。其中，[ r ]代表可读（read）、[ w ]代表可写（write）、[ x ]代表可执行（execute）。要注意的是，这3 个权限的位置不会改变，如果没有权限，就会出现减号[ - ]。第一组所有者权限。第二组所属组权限。第三组其他用户权限（其他用户）
●一个用户只能有一个主组
●【注：权限都可以用数字来代替，例如 rwx 可用 4+2+1 =7 来表示，rw可用数字6来表示等】
●更改文件属性
●更改文件属组
●chgrp   [选项]   属组名    文件名   
●-R              递归处理
●-v               显示执行过程
●-f                不显示错误信息
●更改文件属主和属组
●chown    [选项]    属主名: 属组名   文件名
●-R              递归处理
●-v               显示执行过程
●-f                不显示错误信息
●更改文件权限
●chmod  [选项]   权限    文件名
●-R              递归处理
●-v               显示执行过程
●-f                不显示错误信息
●常见的处理文件和目录的命令
●ls                              列出目录
●cd                            切换目录
●pwd                        显示当前所在位置
●mkdir                     创建新目录                 mkdir   -p        /新目录1/新目录2    （递归创建-p）
●rmdir                      删除空目录（只能删除空的）
●cp                            复制文件或目录(可以同时修改名字)             cp     a.txt     /test/b.txt
●scp                          远程复制文件(多台计算机之间复制)
●wget                       下载文件     wget  -P  /保存文件目录  文件下载地址     下载到指定目录
●rm                             删除文件和目录               rm     -rf     /test1/test2/a.txt    （-r  递归删除，-f不显示错误信息）
●mv                             对文件和目录重命名，或移动文件或目录                mv     /test/a.txt             /test/b.txt
●tar
●压缩文件   (加v显示过程)
●tar –cvf     jpg.tar          *.jpg         //将目录里所有jpg文件打包成jpg.tar
●tar –czf     jpg.tar.gz     *.jpg       //将目录里所有jpg文件打包成jpg.tar后，并且将其用gzip压缩，生成一个gzip压缩过的包，命名为jpg.tar.gz
●tar –cjf      jpg.tar.bz2  *.jpg        //将目录里所有jpg文件打包成jpg.tar后，并且将其用bzip2压缩，生成一个bzip2压缩过的包，命名为jpg.tar.bz2
●tar –cZf     jpg.tar.Z       *.jpg   //将目录里所有jpg文件打包成jpg.tar后，并且将其用compress压缩，生成一个umcompress压缩过的包，命名为jpg.tar.Z
●解压文件   -C指定解压位置
●tar    -xzvf      jpg.tar.gz       -C      /test/img/
●四、列出文件系统的整体磁盘容量情况
●检查文件系统的磁盘空间占用情况
●df     [选项]      [目录或文件名]   (可以利用该命令来获取磁盘被占用了多少空间以及目前还剩下多少空间等信息)
●-a                  包含全部的文件系统
●-h                  以可读性较高的方式显示信息
●du命令也是查看使用空间的，但是与df 命令不同的是，du 命令是对文件和目录磁盘使用的空间的查看
●du    [选项]      [文件]
●-a                   显示个别文件大小
●-h           以 K、M、G 为单位，提高信息的可读性
●-s          仅显示总计，只列出最后加总的值
●-m          以 MB 为单位输出
●磁盘分区
●fdisk    磁盘分区表操作命令
●fdisk    -l                    列出所有分区信息
●fdisk     /根目录所在磁盘位置                     找出磁盘根目录所在的磁盘  (   df     /   )，并展示相关信息
●挂载新硬盘
●lsblk    查看分区情况
●根据提示   n    表示添加新的分区 。其中可以分主分区、扩展分区、逻辑分区（建立在扩展分区中，可以有若干个）。
●主分区编号1-4。逻辑分区编号从5开始，扩展分区不能进行格式化存储数据，且只能有一个。
●离开fdisk工具时按下q，那么所有的动作都不会生效。相反，按下w就是动作生效的意思。
●第一步：添加新硬盘   重启reboot
●第二步：建立分区
●fdisk    /dev/sdb       进入分区工具
●n                                       创建新分区
●p                                       输入分区类型        p   主分区    e扩展分区
●1                                         输入分区号           1-4
●起始扇区 用默认值
●+10G                                 分配空间大小
●可以继续进行分区     可以按   w保存退出     按q不保存退出
●第三步：格式化磁盘，写入文件系统
●mkfs    -t      xfs     /dev/sdb1             -t表示指定文件系统       xfs为文件系统名称 (centos7 默认使用xfs    centos6默认使用ext4     centos5是ext3)
●第四步：挂载     （相当于添加超链，提供一个进入此磁盘的入口）
●mkdir   /newdisk      创建一个新目录
●mount /dev/sdb1    /newdisk     进行挂载
●vim   /etc/fstab     修改配置项 使其永久有效。添加一条如下

●重启之后通过    df -h /dev/sdb1   查看挂载及使用情况